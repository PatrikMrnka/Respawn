{
    "ConnectionStrings": {
        "DefaultConnection": "server=localhost;port=3306;database=lanpartydb;user=lanparty_user;password=uzivatelske_heslo"
    },
    "MySqlSettings": {
        "ServerVersion": "8.0.42"
    },
    "JwtSettings": {
        "Key": "TotoJeVelmiBezpecnyKlicKteryMaRozhodneViceNez32BytuProHmacSha256",
        "Issuer": "RespawnApi",
        "Audience": "RespawnApp",
        "DurationInMinutes": 60
    },
    "DockerSettings": {
        "DockerApiUri": "tcp://localhost:23750"
        // connecting to Docker API - port forward  ssh -N -L localhost:23750:/var/run/docker.sock optimus(ssh client) for example
    },
    "EnvironmentURL": {
        "Frontend": "http://localhost:5173", // URL of the frontend application, e.g. from `npm run dev` command
        "Docker":  "172.20.10.5" // IP address of the Docker host, e.g. from `ifconfig` or `ip a` command
    },
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*"

}
